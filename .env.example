# MySQL connection string used by prisma/schema.prisma and the driver adapter
DB_URL=

# Generate one here: https://generate-secret.vercel.app/32 (only required for localhost)
NEXTAUTH_SECRET=

# Shared secret required by POST /api/auth/register
REGISTER_SECRET=

# Bearer token required by the write endpoints under /api
API_KEY=
